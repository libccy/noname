import { lib, game, ui, get, ai, _status } from '../noname.js';
export const type = 'mode';
/**
 * @type { () => importModeConfig }
 */
export default () => {
	return {
		name: "realtime",
		start() {},
		game: {},
	}
}
