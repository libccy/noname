const characterFilters = {
	// leitong(mode) {
	// 	return mode != "identity" && mode != "guozhan";
	// },
	// wulan(mode) {
	// 	return mode != "identity" && mode != "guozhan";
	// },
};

export default characterFilters;
