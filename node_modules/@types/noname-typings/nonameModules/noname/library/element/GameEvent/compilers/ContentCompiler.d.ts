import IContentCompiler, { EventCompileable, EventCompiledContent } from "./IContentCompiler.ts";
declare class ContentCompiler {
    #private;
    /**
     * ```plain
     * 注册一个编译器实例
     *
     * 如果后面开始全面迁移到 TypeScript，那么请使用依赖注入代替这个方法喵
     * ```
     *
     * @todo 应该使用依赖注入替代
     * @param compiler 编译器实例对象
     */
    addCompiler(compiler: IContentCompiler): void;
    /**
     * ```plain
     * 对无法直接编译的数据做处理
     * ```
     *
     * @param content
     * @returns
     */
    private regularize;
    /**
     * ```plain
     * 集成的编译函数
     * 通过责任链模式将content分发给所有注册的编译器喵
     * ```
     *
     * @param content
     */
    compile(content: EventCompileable): EventCompiledContent;
}
declare const compiler: ContentCompiler;
export default compiler;
